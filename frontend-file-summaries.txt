{
  "frontend/src/index.css": "Global styles with Naturavini brand design system — OKLCH color tokens for olive green primary, tangerine orange accent, cream background, Playfair Display serif + Inter sans-serif fonts, and utility classes for country banners and wine rows.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Playfair Display/Inter font families, olive and tangerine color aliases, catalogue box-shadow, and all standard shadcn/ui tokens mapped to OKLCH CSS variables.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all wine CRUD operations — `useAddWine` and `useUpdateWine` now include the `year: string | null` field in their mutation payloads, passing it through to the backend `addWine` and `updateWine` calls.",
  "frontend/src/components/PageHeader.tsx": "Olive green page header updated to use the new v2 Natura Vini logo (`naturavini-logo-v2.dim_400x200.png`) with an `onError` fallback that renders plain \"NATURA VINI\" text if the image fails to load.",
  "frontend/src/components/WineFormModal.tsx": "Dialog modal for adding/editing a wine entry — now includes an optional `year` field (positioned after Wine Name, before Grape Variety/Wine Style) in both the `WineFormData` interface and the form UI, pre-populated from `initialData.year` when editing.",
  "frontend/src/components/DeleteWineDialog.tsx": "AlertDialog confirmation component for deleting a wine, showing the wine name and winery with confirm/cancel actions and loading state.",
  "frontend/src/components/WineTable.tsx": "Main wine catalogue table — fixed `DeleteWineDialog` usage to pass `wine={deleteTarget}` instead of the incorrect `wineName`/`winery` props; also includes Year column in the UI table and PDF export, v2 logo in PDF, and full year field support in add/edit handlers.",
  "frontend/src/pages/WineList.tsx": "Wine list page with a serif heading, subtitle, admin mode indicator, and the WineTable component.",
  "frontend/src/App.tsx": "Root app component composing PageHeader, WineList page, footer with dynamic year and caffeine.ai attribution, and the Sonner toast provider."
}