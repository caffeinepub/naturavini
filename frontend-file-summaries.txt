{
  "frontend/src/index.css": "Global styles with Naturavini brand design system — OKLCH color tokens for olive green primary, tangerine orange accent, cream background, Playfair Display serif + Inter sans-serif fonts, and utility classes for country banners and wine rows.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Playfair Display/Inter font families, olive and tangerine color aliases, catalogue box-shadow, and all standard shadcn/ui tokens mapped to OKLCH CSS variables.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all wine CRUD operations — fixes the critical bug where `getAllWines()` was called instead of the correct `getWines()` backend method, and adds toast notifications (success and error) for all mutations.",
  "frontend/src/components/PageHeader.tsx": "Olive green page header updated to use the new 400×160 Natura Vini logo image (slightly taller display size), with the admin login/logout button and subtitle bar unchanged.",
  "frontend/src/components/WineFormModal.tsx": "Dialog modal for adding/editing a wine entry — removed the manual DialogPortal + DialogOverlay wrapper (which was doubling up with the built-in shadcn overlay and causing a near-black screen), now uses DialogContent directly with a solid opaque white/cream background and a standard semi-transparent scrim from the shadcn default overlay.",
  "frontend/src/components/DeleteWineDialog.tsx": "AlertDialog confirmation component for deleting a wine, showing the wine name and winery with confirm/cancel actions and loading state.",
  "frontend/src/components/WineTable.tsx": "Main wine catalogue table with country-grouped sections (using normalized keys to prevent duplicate country headers), admin CRUD controls, PDF export with logo, and corrected prop usage matching the actual `WineFormModal` (`onClose`, `initialData`) and `DeleteWineDialog` (`wine`, `onClose`) interfaces.",
  "frontend/src/pages/WineList.tsx": "Wine list page with a serif heading, subtitle, admin mode indicator, and the WineTable component.",
  "frontend/src/App.tsx": "Root app component composing PageHeader, WineList page, footer with dynamic year and caffeine.ai attribution, and the Sonner toast provider."
}